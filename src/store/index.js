import {defineStore} from 'pinia'import request from '@/utils/request'import {FileURL} from '@/utils/Constant'// 第一个参数 容器id,必须唯一export const useMainStore = defineStore('main', {  state: () => {    return {      file_id: 0,      status: 1,      isDark: false,      fileList: [],      breadList: [],      text: '文件',      operationList: [],      dragMoveList: [],      dirMoveList: [],      dirBreadList: [],      createDirName: '',      parent_id: 0,      query: '',      rename: '',      sharePassword: '',      isShare: false,      shareId: '',      updateUserDetailModal: false,    }  },  getters: {  }  ,  actions: {    init(){      this.$patch(state => {        request.post(FileURL, {fileId: state.file_id, status: state.status}).then(res => {          if (res.data.code === 0) {            state.fileList = res.data.data            state.breadList = res.data.bread            state.operationList.length = 0          }        })      })    },    toDir(id){      this.$patch(state => {        state.parent_id = state.file_id        state.file_id = parseInt(id)        this.init()      })    },    inShareDir(id){      this.$patch(state => {        state.file_id = parseInt(id)        this.init()      })    }  },})